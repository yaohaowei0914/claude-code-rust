/**
 * Placeholder barrel for the shell-output migration slice.
 *
 * Components migrated here should render `ToolResultViewModel` items
 * produced by the adapter layer — with the Bash-specific presentation
 * the sample tree uses — rather than rebuilding the normalization from
 * `FrontendContentBlock[]`.
 *
 * No active exports yet — see Issue 05 (operational panels).
 */
export {}
