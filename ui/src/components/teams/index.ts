/**
 * Placeholder barrel for the team/coordination panel migration slice.
 *
 * New team components should subscribe to the store's `teams` record and
 * drive `team_command` payloads through `useBackend()`. Do not import
 * directly from `ui/examples/upstream-patterns/`.
 *
 * No active exports yet — see Issue 05 (operational panels).
 */
export {}
